services:
  postgres:
    image: postgres:17
    container_name: planner-pg
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: planner
    ports:
      - '5432:5432'
